{
  "name": "mintondex-subgraph",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:hosted": "graph deploy --product hosted-service BrunoMarshall/mintondex",
    "deploy:studio": "graph deploy --studio mintondex"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.51.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}
